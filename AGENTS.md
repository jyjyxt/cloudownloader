# 项目协作规则

## 项目概况

- Cloudl 是用于网站适配器、浏览器自动化和媒体下载的 CLI。
- 统一使用 `cloudl` 命令、`@jyjyxt/cloudl` 包名、`~/.cloudl` 配置目录、`CLOUDL_*` 环境变量和 `cloudl-*` skill 名称。
- CLI 核心位于 `src/`，站点适配器位于 `clis/`，浏览器扩展位于 `extension/`，文档位于 `docs/`，集成测试位于 `tests/`。
- 核心使用 TypeScript 严格模式和 ESM；运行环境及依赖版本以对应的 `package.json` 为准。

## 只支持最新写法

- 本项目只支持当前确定的最新接口、命令、参数、配置格式和协议，不要求向后兼容。
- 修改接口或替换写法时，直接采用新方案，并同步更新受影响的调用方、测试、示例、帮助信息和文档。
- 不为旧写法添加或保留兼容别名、旧参数解析、双格式支持、兼容包装、旧协议回退或自动迁移逻辑。
- 遇到已废弃的输入，应明确报错并提示当前写法，不静默转换成新格式。
- 重构涉及的旧实现应随替代方案一起删除，不因兼容历史用法而保留重复路径；与当前任务无关的历史代码不做全仓顺带清理。
- “最新”指项目当前采用的规范，不代表每次任务都升级依赖或提高运行环境最低版本。

## 开发约定

- 先阅读相关实现和附近测试，保持变更聚焦，保留他人已有修改。
- 所有 shell 命令使用 `rtk` 前缀；不支持的子命令通过 `rtk proxy <command>` 执行。
- 修改源码，不直接手改 `dist/` 等构建产物；命令清单通过构建脚本生成。
- 适配器通过 `@jyjyxt/cloudl/registry` 注册，复用现有浏览器、下载和 pipeline 能力。
- 命令的主要必填目标使用位置参数，筛选、分页、格式等可选配置使用命名选项。
- 涉及用户可见行为时，同步维护相关中英文文档及示例。

## 验证

根据变更范围执行必要检查，具体脚本以 `package.json` 为准：

```bash
# CLI 类型检查、默认测试与构建
rtk npm run typecheck
rtk npm test
rtk npm run build

# 仅验证站点适配器
rtk npm run test:adapter

# 扩展需独立检查和构建
rtk npm --prefix extension run typecheck
rtk npm --prefix extension run build
```

- 优先运行受影响的测试；涉及真实浏览器或外部站点的验证按需执行，参考 `TESTING.md`。
- 仅修改文档时检查内容和差异即可，无需运行代码测试。
- 交付时说明实际修改、已执行的验证，以及未能验证的部分。
